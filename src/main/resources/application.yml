management:
  endpoint:
    enabled-by-default: true
  endpoints:
    web:
      exposure:
        include: "*"

spring:
  application:
    name: settings-manager
  datasource:
    url: jdbc:postgresql://localhost:5432/joposcragent
    username: postgres
    password: postgres

settings:
  sentence-transformer:
    base-url: http://localhost:8000/sentence-transformer
